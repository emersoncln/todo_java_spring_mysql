CREATE TABLE IF NOT EXISTS users (id BIGINT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(100) NOT NULL, employee BIGINT NOT NULL);
CREATE TABLE IF NOT EXISTS todos (id BIGINT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(100)  NOT NULL, checked  BOOLEAN NOT NULL);